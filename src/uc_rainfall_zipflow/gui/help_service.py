from __future__ import annotations

DEFAULT_GUI_HELP_TEXT = """【流域雨量グラフ作成 ヘルプ】

この画面では、ZIPまたはExcelから流域雨量グラフを作成できます。

【30秒クイックスタート】
1. モードを選ぶ（解析雨量データ / Excelデータ）
2. 入力元と出力先を指定する
3. 必要な出力種別を選ぶ（通常は「整形時系列グラフ」）
4. 「処理を実行」を押す

【モード別の使い分け】
- 解析雨量データ:
  ZIP群から対象期間を判定して、流域ごとの出力を作成します。
- Excelデータ:
  シート時系列からイベント候補を選択してグラフ出力します。

【主な設定項目】
- 対象流域:
  出力対象の流域を選択します。
- 出力種別:
  ラスタ、整形時系列グラフ、CSVなどを選択します。
- グラフ指標:
  「重み付き合計」「流域平均」を切り替えます。

【画像マージ】
- 「実行後に画像マージ」:
  実行完了後に plots_reference PNG を自動でマージします。
- 「今すぐ画像マージ」:
  既存PNGをその場でマージします。
- レイアウト:
  「列数」「行数」で指定します（初期値 2列 x 4行）。
  最終ページに余りがある場合、余白は空欄のまま出力されます。

【グラフスタイル調整】
- 「グラフスタイル調整」から見た目を変更できます。
- 保存して閉じると、次回以降の出力にも反映されます。

【よくあるエラーと対処】
- 入力ファイルが見つからない:
  指定パスの存在と読み取り権限を確認してください。
- Excel期間不一致:
  シート名の日付と時刻列(B列)の期間整合を確認してください。
- 画像マージ対象なし:
  先に整形時系列グラフ（PNG）を出力してください。
"""


def get_gui_help_text() -> str:
    return DEFAULT_GUI_HELP_TEXT.strip()
